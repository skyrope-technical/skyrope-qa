name = "skyrope-qa"
compatibility_date = "2024-11-01"

# Static-only site. Submissions go to Zoho CRM Web-to-Lead (see index.html),
# so there is no Worker script, no D1, and no Resend. Cloudflare just serves
# the files in ./public over its CDN with automatic SSL.
[assets]
directory = "./public"
